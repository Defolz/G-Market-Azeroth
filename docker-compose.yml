services:
  bot:
    build: .
    container_name: g-market-azeroth-bot
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-m", "g_market_azeroth.healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
